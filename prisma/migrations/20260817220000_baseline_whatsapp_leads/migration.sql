-- Baseline for an existing PostgreSQL database that already contains the intentionally unmanaged table whatsapp_leads.
-- No Prisma-managed schema changes are applied here. This is a safe no-op baseline for migration history.
SELECT 1;
